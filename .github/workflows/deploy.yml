name: Deploy to ECS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Replace everything below with your actual build/push/deploy steps
      # (build the image, push to ECR, update the ECS service/task definition).
      - name: Placeholder
        run: echo "TODO: wire up the real ECS deploy pipeline"
